{
  "name": "root",
  "private": true,
  "scripts": {
    "clean": "lerna run clean",
    "install": "lerna run install",
    "dist-clean": "lerna exec 'rm -rf node_modules' && rm -rf node_modules",
    "build": "lerna run build"
  },
  "devDependencies": {
    "lerna": "^3.15.0"
  }
}
